package language.java.syntax.expressions

enum class BinaryOperator {
    EQUALS,
    NOT_EQUALS,
    ADD,
    MINUS,
    MULTIPLY,
    DIVIDE,
    MODULO,
    LESS_THAN,
    LESS_THAN_EQUALS,
    GREATER_THAN,
    GREATER_THAN_EQUALS,
    AND,
    OR,
}
